# frontend

Pagina statica dell'installazione (`index.html`). Va deployata sullo
stesso hosting condiviso del backend PHP (vedi `../hosting/README.md`):
il campo `API_URL` è un percorso relativo (`/api`), quindi non richiede
alcuna modifica per-deploy.
